"use client";

import React from 'react';

// Stubbed Board: original implementation used react-beautiful-dnd.
// This file is intentionally simplified to avoid a hard dependency on that library.
export default function Board(): React.ReactElement {
  return (
    <div className="w-screen h-screen flex items-center justify-center text-gray-500">
      Kanban board replaced by integrated KanbanTest component.
    </div>
  );
}
